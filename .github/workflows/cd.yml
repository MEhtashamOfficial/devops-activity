name: CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulate Deployment
        run: echo "Deploying application to staging server..."

      - name: Use Secret
        run: echo "Using deploy key: ${{ secrets.DEPLOY_KEY }}"
